<Window x:Class="STRELA_MED.Views.CallEmployeeSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выбор сотрудника для вызова" Height="500" Width="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Выберите сотрудника для отправки уведомления:" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid x:Name="dgEmployees" Grid.Row="1" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="2*"/>
                <DataGridTextColumn Header="Последний осмотр" Binding="{Binding LastExamDate}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Отмена" Click="Cancel_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Вызвать" Click="Select_Click" Width="100" Background="#27AE60" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>