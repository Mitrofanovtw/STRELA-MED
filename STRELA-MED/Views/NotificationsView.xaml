<UserControl x:Class="STRELA_MED.Views.NotificationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="30">
        <StackPanel>
            <TextBlock Text="ВАШИ УВЕДОМЛЕНИЯ" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

            <Border Background="#FDFEFE" CornerRadius="8" BorderBrush="#EBEDEF" BorderThickness="1" Padding="10">
                <DataGrid x:Name="NotificationsGrid" AutoGenerateColumns="False" IsReadOnly="True" 
                          Background="Transparent" BorderThickness="0" GridLinesVisibility="Horizontal"
                          FontSize="14" RowHeight="40">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Дата назначения" Binding="{Binding AppointmentDate, StringFormat={}{0:dd.MM.yyyy}}" Width="150"/>
                        <DataGridTextColumn Header="Сообщение" Binding="{Binding Message}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextWrapping" Value="Wrap"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <TextBlock Text="* Примечание: Пожалуйста, явитесь в указанный срок с паспортом и медкнижкой." 
                       Foreground="#7F8C8D" FontSize="12" Margin="0,15,0,0" FontStyle="Italic"/>
        </StackPanel>
    </Grid>
</UserControl>