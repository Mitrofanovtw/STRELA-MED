<Window x:Class="STRELA_MED.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:STRELA_MED.ViewModels"
        xmlns:views="clr-namespace:STRELA_MED.Views"
        mc:Ignorable="d"
        Title="АИС Паспорт здоровья - ПО Стрела" Height="750" Width="1200"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Background="#2C3E50" Grid.Column="0">
            <StackPanel Margin="10">
                <TextBlock Text="СТРЕЛА-МЕД" Foreground="White" FontSize="24" 
                           FontWeight="Bold" Margin="0,20,0,40" HorizontalAlignment="Center"/>

                <TextBlock Text="Сотрудник:" Foreground="Gray" FontSize="11"/>
                <TextBlock Text="{Binding CurrentUser.FullName}" Foreground="White" 
               FontWeight="Bold" FontSize="14"/>
                <TextBlock Text="{Binding CurrentUser.Role}" Foreground="#F1C40F" 
               FontSize="12"/>
                
                <Button Content="👥 Реестр персонала" 
                        Command="{Binding ShowEmployeesCommand}"
                        Style="{StaticResource MenuButtonStyle}"
                        Cursor="Hand"/>

                <Button Content="🏥 Медосмотры" 
                        Style="{StaticResource MenuButtonStyle}"
                        Cursor="Hand"/>

                <Button Content="📦 Склад / Аптека" 
                        Command="{Binding ShowInventoryCommand}"
                        Style="{StaticResource MenuButtonStyle}"
                        Cursor="Hand"/>

                <Button Content="📊 Отчетность" 
                        Style="{StaticResource MenuButtonStyle}"
                        Cursor="Hand"/>

                <Separator Margin="0,20" Opacity="0.3"/>
                <Button Content="🚪 Выход" Click="Exit_Click" Background="#C0392B" Foreground="White" Height="40" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <ContentControl Grid.Column="1" Content="{Binding CurrentView}" Background="#ECF0F1">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vm:EmployeeListViewModel}">
                    <views:EmployeeListView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:InventoryViewModel}">
                    <views:InventoryView/>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</Window>