<UserControl x:Class="STRELA_MED.Views.MyExamsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="ИСТОРИЯ МОИХ ОСМОТРОВ" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Exams}" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single"
                  MouseDoubleClick="DataGrid_MouseDoubleClick"
                  Background="White" BorderBrush="#DCDDE1" RowHeight="35">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата" Binding="{Binding ExamDate, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Width="150"/>
                <DataGridTextColumn Header="Тип осмотра" Binding="{Binding ExamType}" Width="150"/>
                <DataGridTextColumn Header="Результат" Binding="{Binding Result}" Width="*"/>
                <DataGridTemplateColumn Header="Действие" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="🔍 Открыть" Foreground="#3498DB" Cursor="Hand" HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>