<UserControl x:Class="STRELA_MED.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Учет медицинских препаратов и расходных материалов" 
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="🔍 Поиск по названию:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#7F8C8D"/>
            <TextBox x:Name="SearchTextBox" Grid.Column="1" Height="30" 
                     VerticalContentAlignment="Center" Padding="5,0"
                     TextChanged="SearchTextBox_TextChanged"/>
        </Grid>

        <DataGrid x:Name="MedicineGrid" Grid.Row="2" AutoGenerateColumns="False" 
          CanUserAddRows="False" Background="White" BorderBrush="#BDC3C7"
          SelectionChanged="MedicineGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Остаток" Binding="{Binding Quantity}" Width="100"/>
                <DataGridTextColumn Header="Ед. изм." Binding="{Binding Unit}" Width="80"/>
                <DataGridTextColumn Header="Срок годности" Binding="{Binding ExpirationDate, StringFormat=\{0:dd.MM.yyyy\}}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" Background="White" Margin="0,20,0,0" Padding="15" CornerRadius="5">
            <StackPanel x:Name="AdminControlPanel" Orientation="Horizontal">
                <StackPanel Margin="0,0,20,0">
                    <Label Content="Выбранный препарат:"/>
                    <TextBox Width="200" IsReadOnly="True" x:Name="SelectedMedicineText"/>
                </StackPanel>
                <StackPanel Margin="0,0,20,0">
                    <Label Content="Количество:"/>
                    <TextBox Width="80" x:Name="ActionQuantity"/>
                </StackPanel>
                <Button x:Name="BtnArrival" Content="➕ Приход" Click="BtnArrival_Click" 
        Width="100" Margin="5,15,5,0" Background="#27AE60" Foreground="White" Cursor="Hand"/>
                <Button x:Name="BtnUsage" Content="➖ Расход" Click="BtnUsage_Click" 
        Width="100" Margin="5,15,5,0" Background="#E67E22" Foreground="White" Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>