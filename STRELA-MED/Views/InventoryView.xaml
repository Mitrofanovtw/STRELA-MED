<UserControl x:Class="STRELA_MED.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Учет медицинских препаратов и расходных материалов" 
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <DataGrid Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False"
                  Background="White" BorderBrush="#BDC3C7">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Остаток" Binding="{Binding Quantity}" Width="100"/>
                <DataGridTextColumn Header="Ед. изм." Binding="{Binding Unit}" Width="80"/>
                <DataGridTextColumn Header="Срок годности" Binding="{Binding ExpirationDate}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Background="White" Margin="0,20,0,0" Padding="15" CornerRadius="5">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,20,0">
                    <Label Content="Выбранный препарат:"/>
                    <TextBox Width="200" IsReadOnly="True" x:Name="SelectedMedicineText"/>
                </StackPanel>
                <StackPanel Margin="0,0,20,0">
                    <Label Content="Количество:"/>
                    <TextBox Width="80" x:Name="ActionQuantity"/>
                </StackPanel>
                <Button Content="➕ Приход" Width="100" Margin="5,15,5,0" Background="#27AE60" Foreground="White" Cursor="Hand"/>
                <Button Content="➖ Расход" Width="100" Margin="5,15,5,0" Background="#E67E22" Foreground="White" Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>